# Save Note (2026-04-01 16:53 KST)

## Completed
- Implemented quick-match flow end-to-end:
  - `quick_matches`, `quick_match_responses` tables
  - quick-match APIs (`create`, `list`, `get`, `accept`, `decline`)
  - city/user websocket alerts for quick-match events
- Implemented optional mingle-linked chat:
  - `chat_rooms.mingle_id` (nullable)
  - mingle chat join API: `POST /chatrooms/mingles/{mingleId}/join`
  - chat room DTO now exposes `mingleId`
- Implemented mingle/mingler feature set and expanded APIs:
  - CRUD for `mingles`, user-scoped CRUD for `minglers`
  - city filter for mingles
  - mingle participant list
  - direct join/leave by mingle path

## Runtime Verification Done
- Local app started with `SPRING_PROFILES_ACTIVE=local`, Liquibase applied latest 2 change sets.
- API verification script passed `23/23` checks including:
  - auth-required behavior
  - quick-match create/decline/accept success path
  - non-eligible responder rejection
  - mingle creation + participant linkage on accept
  - mingle chat join behavior + outsider blocked
  - regular chat without mingle still works
  - chat init with `mingleId` returns mingle-linked room
- Websocket/STOMP verification passed all checks:
  - quick match city topic receives `QUICK_MATCH_CREATED` and `QUICK_MATCH_ACCEPTED`
  - requester user topic receives `QUICK_MATCH_DECLINED` and `QUICK_MATCH_ACCEPTED`
  - chat topic receives message broadcast from `/app/chatrooms/{id}/messages`

## Test Artifacts (Local)
- API run artifacts:
  - `cityId=1`, `quickMatch1=1`, `quickMatch2=2`, `mingleId=1`, `mingleChatRoomId=4`, `regularChatRoomId=5`
- Websocket run artifacts:
  - `cityId=1`, `quickMatch1=3`, `quickMatch2=4`, `chatRoomId=6`

## Next Recommended Steps
- Add integration tests (MockMvc + websocket integration) for the new flows to prevent regressions.
- Consider expiring stale `PENDING` quick matches (scheduled job + status transition).
- If needed, add authorization narrowing for `GET /quick-matches` visibility rules (currently authenticated users can query pending list).
