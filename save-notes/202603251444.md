# Save Note

## Completed

- Added root workflow docs in `AGENTS.md` and `NEXT_STEPS.md`.
- Added repository `.gitignore`.
- Implemented user CRUD in the existing `controller -> facade -> service -> repository` structure.
- Wired MapStruct for Kotlin with `kapt` and added `.editorconfig` for IntelliJ save-time formatting.
- Added reusable custom exception infrastructure and `UserNotFoundException`.
- Made user update requests tri-state:
  omitted field = no change, explicit `null` = set null, value = update.
- Added Liquibase and PostgreSQL configuration with local Docker compose support.
- Split Spring config into shared `application.yaml`, `application-local.yaml`, and `application-main.yaml`.
- Added `Nationality` and `City` entities, repositories, Liquibase schema changes, and seeded reference data.
- Renamed Liquibase changelog files to timestamp-based names.

## Next

- Propagate the new `nationality` relation through user create/update/get APIs, DTOs, and mapping.
- Add API endpoints for nationality and city lookup so the frontend can use seeded reference data.
- Decide whether `username`, `email`, `phone`, and similar user fields should truly remain nullable after the tri-state update change.
- Add request validation and additional custom exceptions for invalid input and duplicate values.
- Add integration tests for user CRUD, partial update semantics, and Liquibase-backed startup.

## Notes

- When entity columns change, the related APIs, DTOs, mappings, service logic, and Liquibase scripts should be updated together.
- Liquibase changelog filenames now follow `yyyyMMddHHmmss-*` ordering.
- Docker was not available in this environment, so compose startup was not runtime-verified here.
