# Save Note (2026-04-01 17:25 KST)

## Completed
- Added `locals` domain support:
  - Liquibase table `locals` (joins `users`, `cities`, unique `(user_id, city_id)`).
  - Entity/repository/service/facade/controller/DTOs/requests/responses/exceptions.
  - CRUD APIs for locals with authenticated user handling (`@UserId`).
- Extended quick-match targeting:
  - Added `QuickMatchTargetType` (`MINGLERS`, `LOCALS`, `ANY`).
  - Added `target_type` column to `quick_matches` via Liquibase.
  - Matching audience resolution:
    - `MINGLERS` -> active travelers by trip/city.
    - `LOCALS` -> local users by city.
    - `ANY` -> union of both sets.
  - Quick-match accept eligibility now checks target type.
  - Added typed websocket destinations:
    - `/topic/cities/{cityId}/quick-matches/{minglers|locals|any}`
    - existing generic city topic still publishes all.
- Updated user deletion flow to clean related local rows.

## Verification
- `./gradlew compileKotlin -q` passed.
- Runtime API test script passed (`26/26`) including locals CRUD and target-type behavior.
- Runtime websocket script passed for typed topics and `ANY` union behavior.

## Git
- Feature implementation pushed to `main` at commit `dd3d38e`.

## Next Suggested Steps
- Add API documentation examples for locals + targetType quick-match.
- Add integration tests (controller/service level) for:
  - locals duplicate/ownership cases,
  - quick-match responder eligibility by targetType,
  - websocket typed topic routing behavior.
