# Completed

- Reproduced the API behavior with live HTTP calls against a local app run.
- Fixed `@UserId` argument resolution so authenticated trip/chat endpoints no longer fail with `500`.
- Added a Liquibase changelog to sync seeded table sequences after `loadData`, fixing create failures on `nationalities`, `cities`, and `keywords`.
- Fixed user deletion so related trip/chat rows are cleaned up before deleting the user.
- Added explicit OpenAI API key slots to profile configs.
- Wired `application-local.yaml` to import `optional:file:.env.properties`.
- Added `.env.properties` to `.gitignore` and added `.env.properties.example`.

# Current State

- `main` includes the API bug fixes and the local env-properties setup.
- Local secret file format:
  `OPENAI_API_KEY=...`
- A real OpenAI key had previously been written directly into `application-local.yaml` and was removed; rotate that key if it is still active.

# Next

- Continue the PDF-to-backend gap analysis for `2026 세모톤.pdf`.
- Compare current entities/APIs with prototype requirements and implement only the missing backend fields/endpoints that are clearly needed by the prototype.
- Ignore prototype omissions where the backend already does more than the Figma flow.
