spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/semobackend
    username: semo
    password: secret

app:
  openai:
    api-key: ${OPENAI_API_KEY:}
